<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="shortcut icon" href="favicon.ico" type="image/x-icon">
  <title>Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" 
    integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" 
    crossorigin="anonymous" referrerpolicy="no-referrer"/> 

  <style>
    body { margin:0; padding:0; font-family: Arial, Helvetica, sans-serif; font-size:1.2rem; background:#333; color:#fff; }
    h1,h2,h3,h4,h6,p { text-align:center; }
    #dashboard-content { margin:2rem auto; max-width:600px; padding:1rem; background:#444; border-radius:8px; }
    button { display:block; margin:1rem auto; padding:0.5rem 1rem; font:inherit; color:#fff; background:orange; border:none; border-radius:5px; cursor:pointer; }
    footer { margin-top:3rem; text-align:center; }
    /* .welcome-card {
  background: linear-gradient(135deg, #4facfe, #00f2fe);
  color: white;
  padding: 20px;
  border-radius: 12px;
  text-align: center;
  margin-bottom: 20px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.15);
}
.welcome-card h1 {
  margin: 0;
  font-size: 2rem;
}
.welcome-card p {
  margin: 5px 0 0;
  font-size: 1.1rem;
} */

  </style>
</head>

<body> 
  <!-- <h2 id="dashboard-content">Loading User info/profile...</h2>
  -->
  <button id="logout-btn">Log out</button> 



  <h1 class="text-3xl font-bold mb-2">👋 Welcome back, <%= user.name %>!</h1>
  <p class="text-lg text-gray-600">Your personal dashboard is ready.</p>

  <br><hr><br>
 
  <div class="welcome-card">
    <h1>🌟 Hello <%= user.name %>!</h1>
    <p>We’re glad to see you again. Let’s get started 🚀</p>
  </div>

  <br><hr><br>

  <h2 class="text-2xl font-semibold">Welcome, <%= user.name %> 🎉</h2>
<p class="mt-2">You’re now securely logged in to your IdyllAc dashboard.</p>

<br><hr><br>

  <h2>Welcome, <%= user.name %>!</h2>

  <h1>Welcome to Your Dashboard</h1> 
  <p>You're now securely logged in.</p>

  <h2>How can I help you?</h2>
  <h3>You are about to make an order of IdyllAc card.<br>
      To do that: Provide your Personal Information and queries:<br>
      Fill the form, upload documents, take selfie, and send.</h3> 

  <p><a href="/submit/personal_info" class="btn">Continue to Personal Info Form</a></p>

  <footer>
    <h4><span class="nowrap">&copy;<time id="year"></time> IdyllAc</span></h4>
  </footer> 


  <script>
    // ✅ Refresh access token using cookie refreshToken
    async function refreshAccessToken() {
      try {
        const res = await fetch('/api/auth/refresh-token', {
          method: 'POST',
          credentials: 'include', // 👈 send/receive cookies
          });
  
        if (!res.ok) throw new Error('Failed to refresh token');
          const data = await res.json();
           console.log('🔑 Refreshed access token');
          return data.accessToken; // ✅ return only the new access token in JSON
      } catch (err) {
        console.error('Refresh request failed:', err);
        window.location.href = '/login';
      }
    }
  
    // ✅ Load dashboard info info with fallback on expired access token
    async function loadDashboard() {
      try {
      let res = await fetch('/api/auth/dashboard', {
        method: 'GET',
        credentials: 'include', // 👈 send cookies (access token in Authorization header if you use it)
      });

       // Handle expired/invalid token
      if (res.status === 401 || res.status === 403) {
        console.log('⚠️ Access expired, refreshing...');
        await refreshAccessToken();

         // retry request after refreshing
        res = await fetch('/api/auth/dashboard', {
          method: 'GET',
          credentials: 'include'
        });
      }
  
      if (!res.ok) throw new Error('Failed to load dashboard');
      
        const data = await res.json();
        document.getElementById('dashboard-content').innerHTML = `
          <h2>Hello, ${data.user.name}</h2>
          <p>Email: ${data.user.email}</p>
          <p>User ID: ${data.user.id}</p>
          `;
    } catch (err) {
      console.error('Dashboard load error', err);
      window.location.href = '/login';
    }
   }
  
    // Auto-refresh access token every 14 minutes
    setInterval(refreshAccessToken, 14 * 60 * 1000);

    // Logout
   // public/js/dashboard.js
const logoutBtn = document.getElementById("logout-btn");

if (logoutBtn) {
  logoutBtn.addEventListener("click", async () => {
    try {
      // Try JWT logout first
      let res = await fetch("/api/auth/logout", {
        method: "POST",
        credentials: "include",
      });

      if (res.ok) {
        // Expect JSON with redirect
        const data = await res.json();
        if (data.redirect) {
          window.location.href = data.redirect;
          return;
        }
      }

      // If JWT logout failed, fallback to session logout
      res = await fetch("/logout", { method: "GET", credentials: "include" });

      if (res.redirected) {
        window.location.href = res.url;
      } else {
        window.location.href = "/login"; // fallback
      }
    } catch (err) {
      console.error("Logout error:", err);
      window.location.href = "/login"; // ultimate fallback
    }
  });
}
  </script>
</body>
</html>

